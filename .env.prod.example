# Copia a .env en el servidor de producción y rellena los valores.
# Uso: docker compose -f docker-compose.prod.yml up -d

SECRET_KEY=genera-una-clave-secreta-segura
ALLOWED_HOSTS=condocdat.netgogo.cl

# PostgreSQL (mismo servidor que Talleres)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=condocdat_db
DB_USER=maxgonpe
DB_PASSWORD=tu_password_postgres
DB_HOST=postgres_talleres
DB_PORT=5432

# Correo (obligatorio para «Enviar correo»). Mismo .env que usa compose en el servidor.
EMAIL_HOST=smtp.office365.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=max.gonzalez@propamat.cl
EMAIL_HOST_PASSWORD=tu_contraseña
DEFAULT_FROM_EMAIL=max.gonzalez@propamat.cl
